<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Ported from android-app/app/src/main/res/values/themes.xml, used by
         the native IncomingCallActivity (Compose UI, MaterialTheme handles
         actual theming; this is just the window-level platform theme). -->
    <style name="Theme.HAPhoneTest" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Flutter's own template themes for MainActivity/FlutterActivity. -->
    <style name="LaunchTheme" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
