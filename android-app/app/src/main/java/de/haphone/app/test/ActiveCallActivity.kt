package de.haphone.app.test

import android.os.Bundle
import androidx.activity.ComponentActivity

// Minimal placeholder so Task 2 (OutgoingCallActivity) compiles standalone.
// Task 3 of Plan 06 replaces this with the full Active Call screen
// (Mute/Hold/Audio Routing/Keypad/Transfer/End Call, CALL-01..05).
class ActiveCallActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
