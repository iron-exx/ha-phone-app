---
phase: 01-push-wakeup-proof-of-concept
plan: 05
type: execute
wave: 2
depends_on: ["03"]
files_modified:
  - android-app/app/build.gradle.kts
  - android-app/app/google-services.json
  - android-app/app/src/main/java/de/haphone/app/test/MainActivity.kt
  - .gitignore
  - tools/docs/MANUAL_TEST_PROCEDURE.md
autonomous: false
requirements: [PUSH-03, PUSH-04]

user_setup:
  - service: firebase
    why: "FCM requires a real Firebase project + google-services.json to deliver messages to a real device"
    dashboard_config:
      - task: "Create a Firebase project, add an Android app with package name de.haphone.app.test, download google-services.json"
        location: "https://console.firebase.google.com"
      - task: "Generate a Firebase service-account JSON (for tools/push_trigger.py's firebase-admin sender) under Project Settings -> Service Accounts -> Generate new private key"
        location: "https://console.firebase.google.com -> Project Settings -> Service Accounts"
  - service: google-play-console
    why: "PUSH-04 requires the 'calling app' full-screen-intent declaration, which has no public API and must be done in the Play Console UI (RESEARCH.md Open Question #2)"
    dashboard_config:
      - task: "Create (or confirm) an app listing for de.haphone.app.test on an Internal Testing track"
        location: "https://play.google.com/console"
      - task: "Complete the 'calling app' full-screen-intent declaration under App content"
        location: "Play Console -> App content -> (declarations section covering full-screen intent / calling app), per RESEARCH.md's Pitfall 3 -- confirm the exact click-path directly in the console, since sources disagree on the precise UI labels"

must_haves:
  truths:
    - "The Android app is wired to a real Firebase project and can receive a real FCM message on the test Pixel device"
    - "The Play Console 'calling app' / full-screen-intent declaration is completed and evidenced (screenshot or confirmation text saved), satisfying PUSH-04's declaration requirement"
    - "The developer has run the manual test procedure (tools/docs/MANUAL_TEST_PROCEDURE.md) on the Pixel across foreground/backgrounded/locked/terminated/overnight states and logged pass/fail + rough latency"
    - "The Phase 1 device-matrix scope (D-03, Pixel-only) is reflected in the completed manual test log -- no non-Pixel OEM row is fabricated or implied"
  artifacts:
    - path: "android-app/app/google-services.json"
      provides: "Real Firebase project wiring (gitignored, developer-provided)"
    - path: "android-app/app/build.gradle.kts"
      provides: "google-services Gradle plugin applied now that google-services.json exists"
      contains: "com.google.gms.google-services"
    - path: "tools/docs/MANUAL_TEST_PROCEDURE.md"
      provides: "Completed Android result log rows from the real manual test run"
  key_links:
    - from: "android-app/app/build.gradle.kts"
      to: "android-app/app/google-services.json"
      via: "google-services Gradle plugin reads this file to auto-configure FirebaseApp at build time"
      pattern: "google-services"
---

<objective>
Complete the two pieces of PUSH-03/PUSH-04 that cannot be done by Claude alone: the Firebase project that makes FCM delivery real, and the Play Console "calling app" administrative declaration that unlocks the Android 14+ full-screen-intent auto-grant. Then execute the actual manual test procedure from Plan 01 on the real Pixel device.

Purpose: PUSH-04's own acceptance criterion in ROADMAP.md explicitly requires the Play Console declaration as a discrete success condition, not just code -- and STATE.md's blockers section already flags this as a tracked, not-yet-done external dependency.
Output: A working Firebase-connected Android app, a completed Play Console declaration, and a filled-in manual test log for the Android side of Phase 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-CONTEXT.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-RESEARCH.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-03-PLAN.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Firebase project + Play Console "calling app" declaration</name>
  <what-built>Nothing yet -- this task is the external account/console setup that Task 2's code changes and Task 3's manual test depend on. None of it is automatable: Claude has no Firebase/Play Console session and RESEARCH.md's Open Question #2 confirms the exact declaration UI has no public API.</what-built>
  <action>
    1. In the Firebase console, create a project (or reuse an existing one), add an Android app with package name `de.haphone.app.test`, and download `google-services.json`. Place it at `android-app/app/google-services.json` in this repo (it will be gitignored by Task 2, not committed).
    2. In the same Firebase project, go to Project Settings -> Service Accounts -> Generate new private key. Save the downloaded JSON as `tools/keys/firebase-service-account.json` (already covered by the `tools/keys/*` gitignore pattern from Plan 01) -- this is what `tools/push_trigger.py --fcm-credentials` will use to send real FCM messages.
    3. In the Google Play Console, create (or confirm) an app entry for `de.haphone.app.test` on an Internal Testing release track (a draft/unpublished app is sufficient for this per RESEARCH.md Assumptions Log A1 -- verify this directly in the console since it's unconfirmed).
    4. Complete the "calling app" / full-screen-intent declaration under Play Console -> App content. Take a screenshot or copy the confirmation text as evidence.
    5. Add your own Pixel's Google account as an internal tester if prompted, and install the app once via the Play Console's internal testing link (or side-load the debug APK from Plan 03 via `adb install` for now -- Play distribution isn't required until a later phase; the declaration itself is what PUSH-04 needs).
  </action>
  <resume-signal>Reply "done" with a short confirmation once google-services.json and the Firebase service-account key are placed, and the Play Console declaration is completed (paste or describe the confirmation you see), or describe any blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire the real Firebase project into the app + display the FCM token for manual testing</name>
  <files>android-app/app/build.gradle.kts, android-app/app/src/main/java/de/haphone/app/test/MainActivity.kt, .gitignore</files>
  <read_first>
    - android-app/app/build.gradle.kts (Plan 03 -- root `build.gradle.kts` already declares `com.google.gms.google-services` as `apply false`; this task applies it in the app module now that `google-services.json` exists from Task 1)
    - android-app/app/src/main/java/de/haphone/app/test/MainActivity.kt (Plan 03 stub -- extend, don't replace wholesale)
  </read_first>
  <action>
    Update `.gitignore`, add: `android-app/app/google-services.json` (this is developer/project-specific Firebase config, kept out of the public repo consistent with the project's no-hardcoded-config-in-source posture, even though Firebase's own docs consider it non-secret).

    Update `android-app/app/build.gradle.kts`: add `id("com.google.gms.google-services")` to the `plugins {}` block (alongside the existing `com.android.application` and `org.jetbrains.kotlin.android` entries).

    Update `android-app/app/src/main/java/de/haphone/app/test/MainActivity.kt`: on launch, call `FirebaseMessaging.getInstance().token.addOnCompleteListener { task -> if (task.isSuccessful) { val token = task.result; android.util.Log.i("HAPhoneTest", "FCM token: $token") } }` and display the token as selectable `Text` in the Compose UI (or a classic `TextView` with `setTextIsSelectable(true)`) so the developer can copy it directly off-screen for `tools/push_trigger.py --device-token <token>` -- no need to dig through `adb logcat` every time, though the log line is kept as a fallback.
  </action>
  <acceptance_criteria>
    - `grep -c "com.google.gms.google-services" android-app/app/build.gradle.kts` returns 1 (in the app module's plugins block, not just the root)
    - `test -f android-app/app/google-services.json` succeeds (Task 1's file is present)
    - `git check-ignore android-app/app/google-services.json` exits 0
    - `grep -c "FirebaseMessaging.getInstance().token" android-app/app/src/main/java/de/haphone/app/test/MainActivity.kt` returns at least 1
    - `cd android-app && ./gradlew assembleDebug 2>&1 | tail -30` shows `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/roto/projects/ha-phone-app/android-app && ./gradlew assembleDebug</automated>
  </verify>
  <done>App module applies the google-services plugin against a real, gitignored google-services.json; MainActivity surfaces the device's FCM registration token for the manual test procedure; project still builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the manual test procedure on the Pixel (Android side of D-09)</name>
  <what-built>A real Firebase-wired, Telecom-registered, CallStyle-capable Android app installed on the Pixel test device, ready to receive real signed test pushes from tools/push_trigger.py.</what-built>
  <how-to-verify>
    1. Install the debug APK on the Pixel: `adb install android-app/app/build/outputs/apk/debug/app-debug.apk`.
    2. Launch the app once, copy the displayed FCM token (Task 2).
    3. Follow `tools/docs/MANUAL_TEST_PROCEDURE.md` exactly: for each app state (foreground, backgrounded, locked, terminated, overnight standby), run `python3 tools/push_trigger.py --platform android --state <label> --device-token <fcm-token>` and observe whether the CallStyle full-screen incoming-call notification appears with "HA-Phone Testanruf".
    4. Check `adb logcat -s HAPhoneTest` for the `notification shown callId=... isValid=... isExpired=...` log line to confirm the envelope verified correctly end-to-end.
    5. Fill in the Result Log Table in `tools/docs/MANUAL_TEST_PROCEDURE.md` with real pass/fail + rough latency rows for each state tested, including the overnight-standby run as its own long-duration test.
    6. Confirm the Result Log Table's device column says "Pixel" only -- per D-03, do not add or imply any non-Pixel OEM row; if you later obtain a non-Pixel device, that is explicitly a Phase 6 backlog item, not a Phase 1 retrofit.
  </how-to-verify>
  <resume-signal>Reply "approved" once the manual test table is filled in with real results across all 5 app states, or describe what failed (which state, what was observed instead of the expected notification).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Firebase service-account key -> tools/push_trigger.py | A long-lived, highly privileged credential (can send to any device registered to the project) lives on the developer's local disk |
| Play Console declaration -> OS permission grant | An administrative attestation gates a platform-level security permission (full-screen-intent auto-grant) for all future installs of this app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | tools/keys/firebase-service-account.json, android-app/app/google-services.json | mitigate | Both gitignored (Task 2's `.gitignore` update + Plan 01's existing `tools/keys/*` pattern); `git check-ignore` assertion in acceptance criteria |
| T-01-04 | Information Disclosure | FCM token displayed in MainActivity UI | accept | The token is device- and app-scoped, not a secret capable of impersonating the sender; displaying it on a physical device the developer controls for manual copy-paste is low risk for a single-developer Phase 1 prototype |
</threat_model>

<verification>
```
cd android-app && ./gradlew assembleDebug
git check-ignore android-app/app/google-services.json tools/keys/firebase-service-account.json
```
Both must succeed; the manual test log in `tools/docs/MANUAL_TEST_PROCEDURE.md` must show real (not placeholder) rows before this plan is considered complete.
</verification>

<success_criteria>
- Firebase project wired, google-services plugin applied, build succeeds
- Play Console "calling app" declaration completed with evidence saved
- Manual test log filled in for all 5 app states, Pixel-only, per D-03/D-09
</success_criteria>

<output>
After completion, create `.planning/phases/01-push-wakeup-proof-of-concept/01-05-SUMMARY.md`
</output>
