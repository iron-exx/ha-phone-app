---
phase: 01-push-wakeup-proof-of-concept
plan: 06
type: execute
wave: 3
depends_on: ["04", "05"]
files_modified:
  - tools/docs/MANUAL_TEST_PROCEDURE.md
  - .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md
autonomous: true
requirements: [PUSH-01, PUSH-02]

must_haves:
  truths:
    - "01-PHASE-SIGNOFF.md explicitly states that PUSH-01/PUSH-02 are verified ONLY at the Simulator/unit-test level (via Plan 04's CI) -- real physical-device backgrounded/locked/terminated iOS push delivery is UNVERIFIED and blocked on the user's decision to pay for an Apple Developer Program membership (D-11), not silently marked done"
    - "01-PHASE-SIGNOFF.md summarizes Plan 05's D-12 empirical finding on Android full-screen-intent-without-Play-Console (whatever the actual observed result was)"
    - "01-PHASE-SIGNOFF.md explicitly resolves the D-03 device-matrix gap (Pixel-only scope for Phase 1 sign-off), as the resolution to ROADMAP.md's Phase 1 success criterion #3 -- not silently marked satisfied"
    - "tools/docs/MANUAL_TEST_PROCEDURE.md's iOS section records that iOS was verified at CI/Simulator level only for Phase 1 -- no fabricated real-device pass/fail rows are added"
  artifacts:
    - path: "tools/docs/MANUAL_TEST_PROCEDURE.md"
      provides: "iOS section marked CI/Simulator-verified-only; real-device rows explicitly documented as not tested, blocked by D-11"
    - path: ".planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md"
      provides: "Phase 1 completion note: D-11 iOS real-device gap, D-12 Android empirical finding summary, D-03 scope resolution"
  key_links:
    - from: ".planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md"
      to: "tools/docs/MANUAL_TEST_PROCEDURE.md"
      via: "references the CI-only iOS status and the completed Android empirical/result rows as evidence"
      pattern: "MANUAL_TEST_PROCEDURE"
---

<objective>
Confirm Plan 04's iOS Simulator CI is green, honestly record that real physical-device iOS push verification is an accepted, unresolved Phase 1 gap (D-11), and produce the phase-level sign-off note that explicitly resolves the D-03 device-matrix concern and summarizes Plan 05's D-12 empirical Android finding -- rather than leaving any of these three points ambiguous or silently marked satisfied.

Purpose: Phase 1's zero-budget constraint (D-11/D-12) means this phase cannot fully prove PUSH-01/PUSH-02 on real iOS hardware -- the honest, documented gap IS the deliverable here, not a workaround around it.
Output: An updated `tools/docs/MANUAL_TEST_PROCEDURE.md` iOS section and `.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-CONTEXT.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-01-PLAN.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm iOS CI is green and record the iOS section as CI/Simulator-verified-only</name>
  <files>tools/docs/MANUAL_TEST_PROCEDURE.md</files>
  <read_first>
    - tools/docs/MANUAL_TEST_PROCEDURE.md (Plan 01's doc -- contains an example iOS row already; this task replaces the example with an honest status note, it does not fabricate real-device pass/fail results)
  </read_first>
  <action>
    Confirm the `ios-ci.yml` workflow from Plan 04 has a green run: `gh run list --workflow=ios-ci.yml --limit 1 --json conclusion -q '.[0].conclusion'` should print `success`. If `gh` is unavailable in this environment, note that in the SUMMARY and rely on Plan 04's own Task 2 confirmation instead.

    Update `tools/docs/MANUAL_TEST_PROCEDURE.md`: replace the placeholder/example iOS row(s) in the Result Log Table with a dedicated subsection (e.g. `## iOS Status (Phase 1)`) stating plainly:
    - iOS is verified ONLY via `.github/workflows/ios-ci.yml` (Plan 04): the app builds for the iOS Simulator and `EnvelopeVerifierTests`/`PushHandlerTests`/`DiagnosticsLogTests` all pass.
    - Real physical-device push delivery (foreground/backgrounded/locked/terminated/overnight-standby) is **NOT TESTED** for iOS in Phase 1 -- this is a deliberate, accepted gap per D-11 (the user will not pay for an Apple Developer Program membership required for real VoIP push entitlements), not an oversight or a silently-skipped step.
    - This gap will be closed whenever the user chooses to enroll in the Apple Developer Program; at that point the existing Plan 02 app code and the Simulator-verified test suite are expected to carry over largely unchanged -- only signing/distribution infrastructure (a superset of Plan 04's current unsigned CI) would need to be added.
  </action>
  <acceptance_criteria>
    - `grep -c "NOT TESTED\|not tested" tools/docs/MANUAL_TEST_PROCEDURE.md` returns at least 1
    - `grep -c "D-11" tools/docs/MANUAL_TEST_PROCEDURE.md` returns at least 1
    - `grep -c "Simulator" tools/docs/MANUAL_TEST_PROCEDURE.md` returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/roto/projects/ha-phone-app && grep -c "D-11" tools/docs/MANUAL_TEST_PROCEDURE.md</automated>
  </verify>
  <done>MANUAL_TEST_PROCEDURE.md's iOS section honestly states CI/Simulator-only verification and names D-11 as the reason real-device testing did not happen, with no fabricated pass/fail rows.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 1 sign-off note</name>
  <files>.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md</files>
  <read_first>
    - tools/docs/MANUAL_TEST_PROCEDURE.md (this plan's Task 1 + Plan 05's Task 3 -- both platforms' current status/completed result tables)
    - .planning/ROADMAP.md (Phase 1's 4 success criteria -- criterion #3 is the one D-03 affects)
    - .planning/phases/01-push-wakeup-proof-of-concept/01-CONTEXT.md (D-03, D-09, D-11, D-12)
  </read_first>
  <action>
    Create `.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` containing:
    1. A short summary of what was proven: PUSH-01/PUSH-02 structurally verified via Plan 04's Simulator CI (build + unit tests) only -- NOT verified on real iOS hardware; PUSH-03/PUSH-04 verified on the real Pixel via sideloaded APK (Plan 05), including the D-12 empirical full-screen-intent finding.
    2. A "D-11: iOS Real-Device Verification Gap" section stating explicitly: real physical-device push delivery (backgrounded/locked/terminated/overnight-standby) for iOS is UNVERIFIED in Phase 1, because a free "Personal Team" Apple ID cannot receive the Push Notifications entitlement under any circumstance -- this is blocked on the user's decision to pay for an Apple Developer Program membership ($99/yr), not a technical gap in the app code itself. This is a deliberate, developer-acknowledged scope reduction, not a silently-claimed pass.
    3. A "D-12: Android Full-Screen-Intent Empirical Finding" section summarizing Plan 05's actual observed result (works / doesn't work / partially works) for the full-screen-intent-without-Play-Console-declaration question, quoting or closely paraphrasing the Notes column from `tools/docs/MANUAL_TEST_PROCEDURE.md`'s Android rows.
    4. An explicit "ROADMAP Success Criterion #3 Resolution" section quoting ROADMAP.md's original wording ("verified on at least one non-Pixel OEM device") and stating: scoped down to Pixel-only for Phase 1 sign-off per D-03/CONTEXT.md, with non-Pixel OEM coverage tracked as a Phase 6 hardening backlog item.
    5. A "D-09 Qualitative Acceptance" paragraph stating the developer's own judgment for the Android side (reference the filled-in table in `tools/docs/MANUAL_TEST_PROCEDURE.md` as the evidence trail) -- do not invent a numeric pass rate that wasn't part of the actual decision, and do not extend this qualitative judgment to iOS real-device behavior since that was never tested.
    6. A "Carried Forward" list: iOS real-device push verification remains open pending an Apple Developer Program membership decision (D-11); signing key is currently a single dev Ed25519 keypair (Phase 6 will formalize key distribution); no retry/timeout logic exists yet (Phase 4/5); the Android full-screen-intent grant without a Play Console declaration is empirically known-good/known-bad/mixed (per the D-12 finding above) but not formally re-verified after any future Play Console enrollment.
  </action>
  <acceptance_criteria>
    - `grep -c "Pixel-only\|non-Pixel" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns at least 1
    - `grep -c "D-09\|qualitative" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns at least 1
    - `grep -c "D-11" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns at least 1
    - `grep -c "D-12" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns at least 1
    - `grep -c "MANUAL_TEST_PROCEDURE" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns at least 1
    - `wc -l < .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns 15 or more
  </acceptance_criteria>
  <verify>
    <automated>cd /home/roto/projects/ha-phone-app && test -f .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md && grep -c "D-11" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md</automated>
  </verify>
  <done>01-PHASE-SIGNOFF.md exists, explicitly resolves the D-03 device-matrix gap and the D-11 iOS real-device verification gap (neither silently marked satisfied), and documents the D-12 Android empirical finding and D-09's qualitative acceptance referencing the real completed test logs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI run status (GitHub Actions) -> phase sign-off documentation | The sign-off note's claims about "what was verified" must accurately reflect Plan 04's Simulator-only CI results and Plan 05's real-device Android results, not overstate real-device iOS coverage that was never performed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Repudiation | .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md, tools/docs/MANUAL_TEST_PROCEDURE.md | mitigate | Acceptance criteria explicitly grep for "D-11"/"NOT TESTED"/"Simulator" language in both files, preventing the sign-off (or the manual test doc) from silently overstating iOS push verification beyond what Plan 04's Simulator-only CI actually proved |
</threat_model>

<verification>
Task 1's automated check confirms the honest iOS-gap language exists in MANUAL_TEST_PROCEDURE.md. Task 2's automated check confirms the sign-off doc exists and contains the required D-03/D-09/D-11/D-12 language.
</verification>

<success_criteria>
- iOS CI confirmed green (or its non-availability documented) and MANUAL_TEST_PROCEDURE.md's iOS section honestly reflects Simulator-only verification
- Phase 1 sign-off note explicitly resolves the D-03 scope question, the D-11 iOS real-device gap, and summarizes the D-12 Android empirical finding
</success_criteria>

<output>
After completion, create `.planning/phases/01-push-wakeup-proof-of-concept/01-06-SUMMARY.md`
</output>
