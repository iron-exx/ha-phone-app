---
phase: 01-push-wakeup-proof-of-concept
plan: 06
type: execute
wave: 3
depends_on: ["04", "05"]
files_modified:
  - tools/docs/MANUAL_TEST_PROCEDURE.md
  - .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md
autonomous: false
requirements: [PUSH-01, PUSH-02]

user_setup:
  - service: apns-auth-key
    why: "tools/push_trigger.py needs a real APNs Auth Key (.p8) to send actual VoIP pushes -- distinct from the App Store Connect API key used for TestFlight uploads in Plan 04"
    env_vars:
      - name: APNS_KEY_ID
        source: "Apple Developer -> Certificates, Identifiers & Profiles -> Keys -> (the new key's) Key ID"
      - name: APPLE_TEAM_ID
        source: "developer.apple.com/account -> Membership -> Team ID (same value used in Plan 04)"
    dashboard_config:
      - task: "Generate a new Key under Certificates, Identifiers & Profiles -> Keys, with the 'Apple Push Notifications service (APNs)' capability enabled. Download the .p8 once and place it at tools/keys/AuthKey.p8 (gitignored per Plan 01's .gitignore pattern)."
        location: "https://developer.apple.com/account/resources/authkeys/list"

must_haves:
  truths:
    - "A real VoIP push, signed and sent via tools/push_trigger.py, wakes the TestFlight-installed app and shows the native CallKit screen across foreground/backgrounded/locked/terminated/overnight-standby states on the developer's own iPhone"
    - "The manual test log for iOS reflects real, repeated observations -- not a single lucky run -- sufficient for the developer's own qualitative D-09 judgment"
    - "The phase sign-off note explicitly states the D-03 Pixel-only/non-Pixel-deferred scope decision as the resolution to ROADMAP.md's Phase 1 success criterion #3 -- it is not silently marked satisfied"
  artifacts:
    - path: "tools/docs/MANUAL_TEST_PROCEDURE.md"
      provides: "Completed iOS result log rows alongside Plan 05's Android rows"
    - path: ".planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md"
      provides: "Phase 1 completion note: D-03 scope resolution, D-09 qualitative judgment, links to both platforms' manual test evidence"
  key_links:
    - from: ".planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md"
      to: "tools/docs/MANUAL_TEST_PROCEDURE.md"
      via: "references the completed result log tables for both platforms as evidence"
      pattern: "MANUAL_TEST_PROCEDURE"
---

<objective>
Complete the real-device iOS half of the manual test procedure (D-09) using the TestFlight build from Plan 04, and produce the phase-level sign-off note that explicitly resolves the D-03 device-matrix concern rather than leaving it ambiguous.

Purpose: This is the actual proof that PUSH-01/PUSH-02 hold on real hardware -- everything before this plan was necessary infrastructure (envelope, app code, CI) to make this possible at all, per D-10.
Output: A filled-in iOS section of `tools/docs/MANUAL_TEST_PROCEDURE.md` and `.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-CONTEXT.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-01-PLAN.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-04-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Generate the APNs Auth Key for real VoIP push sending</name>
  <what-built>Nothing code-side -- this is the last piece of Apple-side account setup, distinct from Plan 04's App Store Connect API key (that one authenticates fastlane/CI to App Store Connect; this one authenticates tools/push_trigger.py to send actual APNs VoIP pushes).</what-built>
  <action>
    1. In Apple Developer -> Certificates, Identifiers & Profiles -> Keys, create a new key with the "Apple Push Notifications service (APNs)" capability enabled.
    2. Download the `.p8` file once (Apple only allows one download) and place it at `tools/keys/AuthKey.p8` in this repo (already covered by Plan 01's `tools/keys/*` gitignore pattern -- confirm with `git check-ignore tools/keys/AuthKey.p8`).
    3. Note the Key ID and your Team ID (same Team ID as Plan 04's `APPLE_TEAM_ID`) for use with `tools/push_trigger.py --apns-key-id <id> --apns-team-id <id>`.
  </action>
  <resume-signal>Reply "done" once tools/keys/AuthKey.p8 exists and you have the Key ID + Team ID noted, or describe any blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the manual test procedure on the real iPhone via TestFlight</name>
  <what-built>A signed, TestFlight-distributed build of the throwaway iOS app (Plans 02 + 04), ready to receive real signed VoIP pushes from tools/push_trigger.py.</what-built>
  <how-to-verify>
    1. Install/update the app via the TestFlight app on your iPhone (Plan 04, Task 3 already confirmed this once -- re-confirm you're on the latest build).
    2. Open the app once, go to the Diagnostics view, copy the current VoIP token (Plan 02, Task 3's "Current VoIP Token" section / Copy Token button).
    3. Follow `tools/docs/MANUAL_TEST_PROCEDURE.md` exactly: for each app state (foreground, backgrounded, locked, terminated, overnight standby), run:
       `python3 tools/push_trigger.py --platform ios --state <label> --device-token <voip-token> --apns-key-path tools/keys/AuthKey.p8 --apns-key-id <key-id> --apns-team-id <team-id>`
       and observe whether the native CallKit incoming-call screen appears with "HA-Phone Testanruf".
    4. For the "terminated" state specifically: force-quit the app first, trigger the push, then reopen the app afterward and check the Diagnostics view's log list (or use "Share Log" to export it) for the `receivedAt`/`reportedAt`/`signatureValid` sequence -- this is the D-10 fallback since there is no live Xcode console for a terminated app.
    5. Fill in the Result Log Table in `tools/docs/MANUAL_TEST_PROCEDURE.md` with real pass/fail + rough latency rows for each state, including the overnight-standby run as its own long-duration test (start before sleep, check the next morning).
    6. Also exercise at least one deliberately malformed/expired test push (omit `--device-token`'s corresponding valid envelope by letting the TTL expire before delivery, or manually edit one field in a captured request if convenient) and confirm the call still briefly appears then ends, per Pitfall 1/PUSH-02's "always report" contract -- note this observation in the log table's Notes column.
  </how-to-verify>
  <resume-signal>Reply "approved" once the manual test table is filled in with real iOS results across all 5 app states (plus the malformed/expired-push observation), or describe what failed (which state, what was observed instead of the expected CallKit screen).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Phase 1 sign-off note</name>
  <files>.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md</files>
  <read_first>
    - tools/docs/MANUAL_TEST_PROCEDURE.md (Task 2 of this plan + Plan 05's Task 3 -- both platforms' completed result tables)
    - .planning/ROADMAP.md (Phase 1's 4 success criteria -- criterion #3 is the one D-03 affects)
    - .planning/phases/01-push-wakeup-proof-of-concept/01-CONTEXT.md (D-03, D-09)
  </read_first>
  <action>
    Create `.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` containing:
    1. A short summary of what was proven: PUSH-01/PUSH-02 on the developer's iPhone via TestFlight (this plan), PUSH-03/PUSH-04 code + Play Console declaration on the Pixel (Plan 05).
    2. An explicit "ROADMAP Success Criterion #3 Resolution" section quoting ROADMAP.md's original wording ("verified on at least one non-Pixel OEM device") and stating: scoped down to Pixel-only for Phase 1 sign-off per D-03/CONTEXT.md, with non-Pixel OEM coverage tracked as a Phase 6 hardening backlog item -- this is a deliberate, developer-acknowledged scope reduction, not a silently-claimed pass.
    3. A "D-09 Qualitative Acceptance" paragraph stating the developer's own judgment (reference the filled-in tables in `tools/docs/MANUAL_TEST_PROCEDURE.md` as the evidence trail) -- do not invent a numeric pass rate that wasn't part of the actual decision.
    4. A "Carried Forward" list: signing key is currently a single dev Ed25519 keypair (Phase 6 will formalize key distribution), no retry/timeout logic exists yet (Phase 4/5), Play Console declaration is complete but the app itself remains unpublished/internal-testing-only.
  </action>
  <acceptance_criteria>
    - `grep -c "Pixel-only\|non-Pixel" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns at least 1
    - `grep -c "D-09\|qualitative" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns at least 1
    - `grep -c "MANUAL_TEST_PROCEDURE" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns at least 1
    - `wc -l < .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md` returns 15 or more
  </acceptance_criteria>
  <verify>
    <automated>cd /home/roto/projects/ha-phone-app && test -f .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md && grep -c "Pixel-only\|non-Pixel" .planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md</automated>
  </verify>
  <done>01-PHASE-SIGNOFF.md exists, explicitly resolves the D-03 device-matrix gap (not silently marked satisfied), and documents D-09's qualitative acceptance referencing the real completed test logs from both platforms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| APNs Auth Key (.p8) -> local disk | A second highly privileged Apple credential (in addition to Plan 04's ASC API key), capable of sending pushes to any device token targeted at this app's topic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Elevation of Privilege | tools/keys/AuthKey.p8 | mitigate | Covered by Plan 01's existing `tools/keys/*` gitignore pattern; acceptance criteria in Task 1 explicitly re-verifies `git check-ignore` before proceeding |
</threat_model>

<verification>
Manual gate (Tasks 1-2). Task 3's automated check confirms the sign-off doc exists and contains the required D-03/D-09 language.
</verification>

<success_criteria>
- Real VoIP push wakes the TestFlight build across all 5 app states, logged in MANUAL_TEST_PROCEDURE.md
- Phase 1 sign-off note explicitly resolves the D-03 scope question
</success_criteria>

<output>
After completion, create `.planning/phases/01-push-wakeup-proof-of-concept/01-06-SUMMARY.md`
</output>
