---
phase: 02-pjsip-audio-media-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ios-app/scripts/build_pjsip_ios.sh
  - ios-app/HAPhoneTestApp/Sip/config_site.h
  - ios-app/project.yml
  - .github/workflows/ios-ci.yml
  - .gitignore
autonomous: false
requirements: [CALL-01]

must_haves:
  truths:
    - "PJSIP 2.17 is built from source for iOS (device + Simulator archs) with Opus compiled in, not a community CocoaPod (D-01)"
    - "The iOS build pipeline runs on a GitHub Actions macOS runner, matching Phase 1's D-02 pattern -- not attempted locally in this Linux sandbox"
  artifacts:
    - path: "ios-app/scripts/build_pjsip_ios.sh"
      provides: "reproducible from-source PJSIP 2.17 iOS build script (device + simulator)"
      contains: "PJSIP_TAG=\"2.17\""
    - path: ".github/workflows/ios-ci.yml"
      provides: "CI step building PJSIP before xcodegen generate"
      contains: "build_pjsip_ios.sh"
  key_links:
    - from: ".github/workflows/ios-ci.yml"
      to: "ios-app/scripts/build_pjsip_ios.sh"
      via: "run: step invoking the script before xcodegen generate"
      pattern: "build_pjsip_ios\\.sh"
    - from: "ios-app/project.yml"
      to: "PjsuaSmokeCheck.mm"
      via: "target sources + bridging header settings referencing the built xcframework"
      pattern: "pjsua2\\.xcframework"
---

<objective>
Build PJSIP 2.17 from source for iOS (per D-01/D-02: official source build, GitHub Actions macOS runner — this Linux sandbox has no Xcode) with Opus enabled, producing a static `.xcframework`, and extend the existing `ios-ci.yml` pipeline to build it before `xcodegen generate`. Add a minimal Objective-C++ smoke file proving the framework links, structurally verified the same way Phase 1's Plan 04 verified CI (grep/YAML checks, since `xcodebuild` cannot run in this sandbox).

Purpose: Wave 1 build-tooling prerequisite for Plan 05 (iOS SIP call controller) — nothing that imports PJSUA2 C++ headers can build until this exists.
Output: `ios-app/scripts/build_pjsip_ios.sh`, `ios-app/HAPhoneTestApp/Sip/config_site.h` (Opus enabled), an extended `.github/workflows/ios-ci.yml`, and `project.yml` wired to link the resulting xcframework.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-pjsip-audio-media-core/02-CONTEXT.md
@.planning/phases/02-pjsip-audio-media-core/02-RESEARCH.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md

<interfaces>
From `.github/workflows/ios-ci.yml` (current, full file — the pipeline to extend):
```yaml
name: iOS CI (Simulator build + unit tests)
on:
  push:
    branches: [main]
    paths: ["ios-app/**"]
  workflow_dispatch: {}
jobs:
  build-test:
    runs-on: macos-14
    defaults:
      run:
        working-directory: ios-app
    steps:
      - uses: actions/checkout@v4
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Generate Xcode project
        run: xcodegen generate --spec project.yml
      - name: Build for iOS Simulator (unsigned)
        run: |
          xcodebuild build \
            -project HAPhoneTestApp.xcodeproj \
            -scheme HAPhoneTestApp \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO
      - name: Run unit tests on iOS Simulator
        run: |
          xcodebuild test \
            -project HAPhoneTestApp.xcodeproj \
            -scheme HAPhoneTestApp \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            CODE_SIGNING_ALLOWED=NO
```

From `ios-app/project.yml` (current, full file — the xcodegen spec to extend):
```yaml
name: HAPhoneTestApp
options:
  bundleIdPrefix: de.haphone.app
  deploymentTarget:
    iOS: "17.0"
targets:
  HAPhoneTestApp:
    type: application
    platform: iOS
    sources: [HAPhoneTestApp]
    info:
      path: HAPhoneTestApp/Info.plist
      properties:
        UIBackgroundModes: [voip]
    entitlements:
      path: HAPhoneTestApp/HAPhoneTestApp.entitlements
      properties:
        aps-environment: development
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: de.haphone.app.test
        CODE_SIGN_STYLE: Automatic
    scheme:
      testTargets: [HAPhoneTestAppTests]
  HAPhoneTestAppTests:
    type: bundle.unit-test
    platform: iOS
    sources: [HAPhoneTestAppTests]
    dependencies:
      - target: HAPhoneTestApp
```

From `01-PHASE-SIGNOFF.md` §2 — the precedent for CI verification limits in this sandbox: `gh` CLI is not installed and this sandbox cannot push to GitHub (no working credential helper). Structural verification (YAML validity + grep-based checks) is the only verification available from here; a checkpoint records that the live run remains unconfirmed until the user pushes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PJSIP 2.17 from source for iOS with Opus enabled (device + Simulator)</name>
  <files>ios-app/scripts/build_pjsip_ios.sh, ios-app/HAPhoneTestApp/Sip/config_site.h, .gitignore</files>
  <read_first>ios-app/project.yml, .github/workflows/ios-ci.yml</read_first>
  <action>
    Create `ios-app/HAPhoneTestApp/Sip/config_site.h`:
    ```c
    // PJSIP config_site.h override for HA-Phone -- enables Opus (RESEARCH.md
    // Pitfall 3: silently omitted otherwise). Included via the build script's
    // configure-iphone step, not part of the app's own compiled sources.
    #define PJMEDIA_HAS_OPUS_CODEC 1
    ```
    Create `ios-app/scripts/build_pjsip_ios.sh` (executed by CI on the macOS runner, per D-02 -- never locally in this Linux sandbox):
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail

    PJSIP_TAG="2.17"
    WORKDIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
    VENDOR_DIR="$WORKDIR/third_party/pjproject"
    CONFIG_SITE="$WORKDIR/HAPhoneTestApp/Sip/config_site.h"
    XCFRAMEWORK_OUT="$WORKDIR/Frameworks/pjsua2.xcframework"

    echo "== Cloning pjproject tag $PJSIP_TAG (pinned, not a moving branch) =="
    if [ ! -d "$VENDOR_DIR" ]; then
        git clone --branch "$PJSIP_TAG" --depth 1 https://github.com/pjsip/pjproject.git "$VENDOR_DIR"
    fi
    cp "$CONFIG_SITE" "$VENDOR_DIR/pjlib/include/pj/config_site.h"

    echo "== Installing Opus via Homebrew (macOS runner) =="
    brew install opus

    cd "$VENDOR_DIR"
    echo "== configure-iphone: device (arm64) =="
    ./configure-iphone --with-opus="$(brew --prefix opus)"
    make dep
    make clean
    make

    echo "== configure-iphone: Simulator (arm64-simulator) =="
    # Per docs.pjsip.org iOS build_instructions.html: DEVICE variable selects
    # the Simulator SDK/arch pass; re-run configure-iphone + make into a
    # separate build products directory so device+simulator libs don't clash.
    DEVICE=iPhoneSimulator ./configure-iphone --with-opus="$(brew --prefix opus)"
    make dep
    make clean
    make

    echo "== Packaging static libs into an xcframework =="
    mkdir -p "$WORKDIR/Frameworks"
    xcodebuild -create-xcframework \
        -library "$VENDOR_DIR/pjsip-apps/lib/libpjproject-arm64-apple-darwin_ios.a" \
        -library "$VENDOR_DIR/pjsip-apps/lib/libpjproject-x86_64-apple-darwin_ios_simulator.a" \
        -output "$XCFRAMEWORK_OUT" \
        || echo "WARNING: exact .a output filenames vary by pjproject version -- inspect $VENDOR_DIR/pjsip-apps/lib/*.a and adjust the -library paths above before re-running"

    echo "== Build-verification gate: Opus actually compiled in =="
    OPUS_OBJ_COUNT=$(find "$VENDOR_DIR" -iname "*opus*.o" | wc -l)
    if [ "$OPUS_OBJ_COUNT" -eq 0 ]; then
        echo "ERROR: no opus object files found -- Opus was NOT compiled in (Pitfall 3)" >&2
        exit 1
    fi
    echo "PJSIP $PJSIP_TAG built for iOS device+simulator, opus object files: $OPUS_OBJ_COUNT"
    ```
    Make it executable (`chmod +x`). Add to the repo root `.gitignore` (create if absent, or append if present):
    ```
    ios-app/third_party/
    ios-app/Frameworks/
    ```
  </action>
  <verify>
    <automated>bash -n ios-app/scripts/build_pjsip_ios.sh && grep -c "PJMEDIA_HAS_OPUS_CODEC 1" ios-app/HAPhoneTestApp/Sip/config_site.h</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n ios-app/scripts/build_pjsip_ios.sh` exits 0 (syntax valid; cannot be executed for real in this Linux sandbox — no Xcode/macOS)
    - `test -x ios-app/scripts/build_pjsip_ios.sh` exits 0
    - `grep -c "PJSIP_TAG=\"2.17\"" ios-app/scripts/build_pjsip_ios.sh` returns 1
    - `grep -c "PJMEDIA_HAS_OPUS_CODEC 1" ios-app/HAPhoneTestApp/Sip/config_site.h` returns 1
  </acceptance_criteria>
  <done>Build script authored and syntax-validated; real execution deferred to the GitHub Actions macOS runner per D-02 (cannot run in this sandbox).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the PJSIP build into ios-ci.yml and project.yml</name>
  <files>.github/workflows/ios-ci.yml, ios-app/project.yml, ios-app/HAPhoneTestApp/Sip/PjsuaSmokeCheck.mm, ios-app/HAPhoneTestApp/Sip/PjsuaSmokeCheck.h</files>
  <read_first>.github/workflows/ios-ci.yml, ios-app/project.yml</read_first>
  <action>
    Edit `.github/workflows/ios-ci.yml`: insert a new step between "Install XcodeGen" and "Generate Xcode project":
    ```yaml
      - name: Build PJSIP 2.17 for iOS (device + simulator)
        run: |
          chmod +x scripts/build_pjsip_ios.sh
          ./scripts/build_pjsip_ios.sh
    ```
    Create `ios-app/HAPhoneTestApp/Sip/PjsuaSmokeCheck.h`:
    ```objc
    // Compile-time smoke check (Plan 03, Task 2): confirms the pjsua2
    // xcframework built by build_pjsip_ios.sh links into the app target.
    // Superseded by PjsuaBridge.h in Plan 05, which owns the real
    // Endpoint/Account/Call wrapper.
    #ifndef PjsuaSmokeCheck_h
    #define PjsuaSmokeCheck_h
    #import <Foundation/Foundation.h>
    BOOL PjsuaBindingsAvailable(void);
    #endif
    ```
    Create `ios-app/HAPhoneTestApp/Sip/PjsuaSmokeCheck.mm`:
    ```objc
    #import "PjsuaSmokeCheck.h"
    #include <pjsua2.hpp>

    BOOL PjsuaBindingsAvailable(void) {
        return pj::Endpoint::instance().libGetVersion() != nullptr;
    }
    ```
    Edit `ios-app/project.yml`'s `HAPhoneTestApp` target: replace the existing single `settings.base` map with this merged version (do NOT create a second `settings:` key — YAML requires exactly one `settings:` mapping per target):
    ```yaml
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: de.haphone.app.test
        CODE_SIGN_STYLE: Automatic
        HEADER_SEARCH_PATHS: "$(SRCROOT)/third_party/pjproject/pjsip/include $(SRCROOT)/third_party/pjproject/pjlib/include $(SRCROOT)/third_party/pjproject/pjmedia/include $(SRCROOT)/third_party/pjproject/pjnath/include $(SRCROOT)/third_party/pjproject/pjlib-util/include $(SRCROOT)/third_party/pjproject/pjsip-apps/src/pjsua2/include"
        CLANG_CXX_LANGUAGE_STANDARD: "gnu++17"
        CLANG_ENABLE_MODULES: NO
    dependencies:
      - framework: Frameworks/pjsua2.xcframework
        embed: false
    ```
    Add `Sip` to the target's `sources` list (currently `sources: [HAPhoneTestApp]` — xcodegen already recurses the `HAPhoneTestApp` directory, so the new `Sip/` subfolder's `.h`/`.mm`/`.swift` files are picked up automatically; no change needed there, but confirm by checking `sources: [HAPhoneTestApp]` still covers `HAPhoneTestApp/Sip/*` after generation in Task 3's CI run).
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ios-ci.yml')); yaml.safe_load(open('ios-app/project.yml')); print('yaml-ok')" && grep -c "build_pjsip_ios.sh" .github/workflows/ios-ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ios-ci.yml'))"` exits 0 (valid YAML)
    - `python3 -c "import yaml; yaml.safe_load(open('ios-app/project.yml'))"` exits 0 (valid YAML, no duplicate `settings:` key)
    - `grep -c "build_pjsip_ios.sh" .github/workflows/ios-ci.yml` returns at least 1
    - `grep -c "pjsua2.xcframework" ios-app/project.yml` returns at least 1
    - The "Build PJSIP" CI step appears strictly before the "Generate Xcode project" step in `.github/workflows/ios-ci.yml` (`grep -n "Build PJSIP\|Generate Xcode project" .github/workflows/ios-ci.yml` shows PJSIP step's line number lower)
  </acceptance_criteria>
  <done>ios-ci.yml builds PJSIP before generating the Xcode project; project.yml links the resulting xcframework and sets Obj-C++ header search paths; both YAML files remain structurally valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the extended iOS CI pipeline actually runs on GitHub</name>
  <action>No further automated action available from this sandbox -- Tasks 1-2 already completed all automatable work (build script authored, CI wired, structural YAML/grep verification passed). Confirming the live GitHub Actions run requires the user to push and check the Actions tab, since this sandbox has no `gh` CLI and cannot push to the GitHub remote (same constraint as Phase 1's 01-PHASE-SIGNOFF.md).</action>
  <what-built>Claude authored and structurally verified (YAML validity + grep checks) the PJSIP-for-iOS build step, the xcframework linkage, and the Obj-C++ smoke file. Per Phase 1's D-11/01-PHASE-SIGNOFF.md precedent, this sandbox has no `gh` CLI and cannot push to GitHub, so the live run has never been confirmed from here.</what-built>
  <how-to-verify>
    1. Push this branch (or merge to `main`) so `.github/workflows/ios-ci.yml`'s `paths: ["ios-app/**"]` trigger fires, or manually trigger via GitHub's Actions tab -> "iOS CI" -> "Run workflow".
    2. Watch the run: confirm "Build PJSIP 2.17 for iOS (device + simulator)" completes successfully (the script's own `OPUS_OBJ_COUNT` gate will fail the step if Opus wasn't compiled in), then confirm "Generate Xcode project" / "Build for iOS Simulator" / "Run unit tests on iOS Simulator" all stay green.
    3. Report back pass/fail and, if it fails, paste the failing step's log output (most likely failure point per RESEARCH.md Open Question 3: the exact SWIG/`.a` output filenames in the xcframework packaging step may need adjusting for the actual pjproject 2.17 layout).
  </how-to-verify>
  <resume-signal>Paste the GitHub Actions run URL + pass/fail per step, or "not yet pushed" if deferring.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> upstream source | `git clone --branch 2.17` pulls third-party C/C++ source that gets compiled and linked into the shipping iOS app on a GitHub-hosted macOS runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Tampering | `build_pjsip_ios.sh` git clone step | mitigate | Pin the exact release tag `2.17` (not a branch), matching D-01 and Plan 02's identical Android mitigation — avoids supply-chain drift from a moving ref |
| T-2-07 | Information Disclosure | Vendored PJSIP source / xcframework build output | accept | `ios-app/third_party/` and `ios-app/Frameworks/` are gitignored — not committed, rebuilt deterministically by CI from the pinned tag on every run |
</threat_model>

<verification>
`bash -n ios-app/scripts/build_pjsip_ios.sh` exits 0; both YAML files parse; PJSIP build step appears before `xcodegen generate` in the workflow. Live CI green run confirmed via Task 3's checkpoint once pushed.
</verification>

<success_criteria>
PJSIP 2.17 build script authored, Opus-enabled, and wired into the existing GitHub Actions iOS CI pipeline ahead of the Xcode project generation step; structurally verified from this sandbox, live-verified via the human checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pjsip-audio-media-core/02-03-SUMMARY.md`
</output>
