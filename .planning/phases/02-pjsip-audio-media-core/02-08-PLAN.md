---
phase: 02-pjsip-audio-media-core
plan: 08
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-06", "02-07"]
files_modified:
  - tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md
  - .planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md
autonomous: false
requirements: [CALL-01, CALL-05]

must_haves:
  truths:
    - "All 3 codecs (Opus, G.722, G.711 alaw/ulaw) are verified for real, two-way, against the HA-Phone box, not just configured (CALL-01, D-07)"
    - "Transient SIP registration (register-on-call-start, unregister-on-call-end) is verified via the corrected Asterisk CLI commands (pjsip show contacts/aor/endpoint), not the non-existent sip show registry (CALL-05, D-10, Pitfall 2)"
    - "Mid-call WiFi-to-cellular network-switch resilience is exercised and its outcome recorded, in scope per D-09"
    - "The iOS real-device audio verification gap is documented with a concrete resumption trigger (Apple Developer Program enrollment), not left as an undated 'someday' item (D-17/D-18)"
  artifacts:
    - path: "tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md"
      provides: "manual test steps + result log for CALL-01/CALL-05, mirroring Phase 1's MANUAL_TEST_PROCEDURE.md"
    - path: ".planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md"
      provides: "phase sign-off documenting what was proven and what remains an accepted gap"
  key_links:
    - from: "tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md"
      to: "02-PHASE-SIGNOFF.md"
      via: "sign-off cites the manual procedure's result log as its evidence trail"
      pattern: "PHASE2_MANUAL_TEST_PROCEDURE"
---

<objective>
Close out Phase 2 the same way Phase 1 closed: run the full automated test suite on both platforms, perform the manual-only verifications that cannot be automated (CALL-01's real 3-codec audio test, CALL-05's transient-registration Asterisk CLI check, D-09's mid-call network-switch test), and write `02-PHASE-SIGNOFF.md` documenting what was proven versus what remains an accepted, named gap — most notably the iOS real-device audio verification gap (D-15/D-16), closed only when the user enrolls in the Apple Developer Program (D-17/D-18).

Purpose: Phase 2 cannot be marked complete on vibes — CALL-01 and CALL-05 are explicitly manual-only per the Validation Architecture, and this plan is where that evidence gets collected and recorded.
Output: `tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md` (test steps + result log table) and `.planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md` (the sign-off document).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-pjsip-audio-media-core/02-CONTEXT.md
@.planning/phases/02-pjsip-audio-media-core/02-RESEARCH.md
@.planning/phases/02-pjsip-audio-media-core/02-VALIDATION.md
@.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md
@.planning/phases/01-push-wakeup-proof-of-concept/tools/docs/MANUAL_TEST_PROCEDURE.md

<interfaces>
From `tools/docs/MANUAL_TEST_PROCEDURE.md` (Phase 1, full file — the structure this plan's new doc mirrors): Device Matrix Scope section, per-test-run numbered steps, a Result Log Table with columns `Date | Platform | Codec/Test | Expected | Observed | Pass/Fail | Notes`, and an explicit "iOS Status" section documenting what could and couldn't be tested.

From `01-PHASE-SIGNOFF.md` (Phase 1, full file — the sign-off structure this plan's new doc mirrors): "What Was Actually Proven" / numbered open-gap sections with named resumption triggers / "ROADMAP Success Criterion Resolution" / "Carried Forward" list.

From `02-RESEARCH.md` Pitfall 2 (the corrected CLI command CALL-05's manual verification must use, NOT `sip show registry` which does not exist on this Asterisk 22 box — `chan_sip` was removed):
```
asterisk -rx "pjsip show contacts"
asterisk -rx "pjsip show aor <extension-number>"
asterisk -rx "pjsip show endpoint <extension-number>"
```

From `02-CONTEXT.md` D-09 (mid-call network-switch handling is in-scope for Phase 2's stability bar, not deferred) and D-18 (the sign-off names a concrete resumption trigger: real iOS audio verification is picked back up specifically when the user enrolls in the Apple Developer Program, $99/yr — not an undated "someday" item).

From `02-VALIDATION.md` Manual-Only Verifications table — the exact 4 rows this plan's procedure doc must cover: real two-way audio over 3 codecs + Bluetooth routing (CALL-01), transient registration (CALL-05), iOS real-device audio path (deferred, tracked here), cross-repo TLS/SRTP test extension boot (Plan 01's Task 3, already checkpoint-verified there — cross-referenced, not re-run here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suites + author the manual test procedure doc</name>
  <files>tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md</files>
  <read_first>tools/docs/MANUAL_TEST_PROCEDURE.md, .planning/phases/02-pjsip-audio-media-core/02-VALIDATION.md</read_first>
  <action>
    Run the full automated suites and capture pass/fail counts:
    ```bash
    cd android-app && ./gradlew testDebugUnitTest --console=plain
    cd ~/projects/Ha-Phone/ha-phone && python3 -m pytest backend/tests/test_api.py backend/tests/test_cont_init_tls.py -x
    ```
    (iOS's `xcodebuild test` cannot run in this Linux sandbox — its status is whatever Plan 03's Task 3 checkpoint reported back; cite that result here rather than re-attempting it.)

    Create `tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md`, mirroring Phase 1's `MANUAL_TEST_PROCEDURE.md` structure:
    ```markdown
    # Manual Test Procedure: PJSIP Audio/Media Core (Phase 2)

    Exercises the real SIP/media path built in this phase against the
    dedicated TLS/SRTP test extension provisioned in Plan 01 (extension
    number and credentials: see Plan 01's Task 3 checkpoint output).

    ## Test Extension (Plan 01)

    | Field | Value |
    |-------|-------|
    | Extension number | <from Plan 01 Task 3 checkpoint> |
    | SIP password | <from Plan 01 Task 3 checkpoint> |
    | Transport | TLS, port 5061 |
    | Media encryption | SDES |

    ## CALL-01: Codec + Audio Routing Test Matrix (D-07)

    For each codec, temporarily zero out the other 3 codecs' priority
    (`Endpoint.codecSetPriority` to 0 for the others, or restrict the test
    extension's Asterisk `allow=` list to a single codec for that run) so
    negotiation is forced onto the codec under test, then place a real
    call and confirm audible, intelligible two-way audio:

    | Codec | Direction | Mic | Speaker | Bluetooth | Pass/Fail | Notes |
    |-------|-----------|-----|---------|-----------|-----------|-------|
    | opus/48000 | MO (outgoing) | | | | | |
    | opus/48000 | MT (incoming) | | | | | |
    | g722/16000 | MO | | | | | |
    | g722/16000 | MT | | | | | |
    | pcma/8000 (alaw) | MO | | | | | |
    | pcmu/8000 (ulaw) | MO | | | | | |

    Also exercise: DTMF digits received correctly at an IVR/echo-test
    extension (CALL-02); hold + blind transfer to a second working
    extension (CALL-04); mid-call WiFi-to-cellular network switch,
    confirming audio survives the handoff (CALL-01 stability bar, D-09,
    per `Endpoint.handleIpChange`).

    ## CALL-05: Transient Registration Verification (D-10, corrected per Pitfall 2)

    `sip show registry` does NOT exist on this Asterisk 22 box (`chan_sip`
    removed). Use instead, via the HA-Phone add-on's terminal/SSH:

    1. Before placing any call: `asterisk -rx "pjsip show contacts"` --
       confirm NO contact exists for the test extension.
    2. Place/answer a call from the app.
    3. During the call: `asterisk -rx "pjsip show aor <extension-number>"`
       and `asterisk -rx "pjsip show endpoint <extension-number>"` --
       confirm a contact now exists.
    4. End the call.
    5. Immediately after: re-run `pjsip show contacts` -- confirm the
       contact is gone again (transient, not persistent).

    ## Result Log Table

    | Date | Platform | Test | Expected | Observed | Pass/Fail | Notes |
    |------|----------|------|----------|----------|-----------|-------|
    | | android | opus MO | audible both ways | | | |
    | | android | g722 MO | audible both ways | | | |
    | | android | pcma/pcmu MO | audible both ways | | | |
    | | android | DTMF | digits received at PBX | | | |
    | | android | hold+transfer | call transfers cleanly | | | |
    | | android | network switch | audio survives handoff | | | |
    | | android | CALL-05 registration | contact exists only during call | | | |
    | | ios | all CALL-01..05 | NOT PERFORMED -- D-15/D-16 Simulator-only gap | n/a | Not performed | Blocked per D-15/D-16 zero-budget constraint; see 02-PHASE-SIGNOFF.md |

    ## iOS Status (Phase 2)

    iOS is verified for Phase 2 ONLY structurally: PJSIP 2.17 builds via
    the GitHub Actions macOS runner (Plan 03), SipCallController's unit
    tests pass on the iOS Simulator (Plan 05), no real device or real
    audio I/O is exercised. See `02-PHASE-SIGNOFF.md` for the full gap
    documentation and resumption trigger (D-17/D-18).
    ```
  </action>
  <verify>
    <automated>cd android-app && ./gradlew testDebugUnitTest --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest` from `android-app/` exits 0
    - `python3 -m pytest backend/tests/test_api.py backend/tests/test_cont_init_tls.py -x` from `~/projects/Ha-Phone/ha-phone` exits 0
    - `grep -c "pjsip show contacts" tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md` returns at least 2 (corrected command, not `sip show registry`)
    - `grep -c "sip show registry" tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md` returns 0 (the non-existent legacy command must never appear as an instruction)
  </acceptance_criteria>
  <done>Full automated suites green on Android and the HA-Phone backend; iOS status cited from Plan 03's CI result; manual test procedure doc authored with the corrected D-10 CLI commands and the full CALL-01/02/04/05 + D-09 test matrix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Perform the real-device manual verification matrix</name>
  <action>No further automated action available from this sandbox -- Task 1 already completed all automatable work (full unit test suites run, manual test procedure doc authored). Real SIP calls, Bluetooth audio routing, and physical WiFi-to-cellular network switching require a real Android device and the real HA-Phone box, which this sandbox does not have.</action>
  <what-built>Task 1 authored the manual test procedure. Claude cannot place real SIP calls, plug in Bluetooth audio, or physically switch a phone from WiFi to cellular from this sandbox -- these require a real Android device and the real HA-Phone box.</what-built>
  <how-to-verify>
    Follow `tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md` end to end on a real Android device (or the API 35 emulator used in Phase 1, if that substitution remains acceptable for this phase too -- note any substitution explicitly, same as Phase 1's sign-off did):
    1. Fill in the Test Extension table from Plan 01's Task 3 checkpoint output.
    2. Run through the CALL-01 codec matrix (6 rows: 3 codecs x MO, plus MT where noted).
    3. Test DTMF, hold+transfer, and mid-call network switch.
    4. Run the CALL-05 Asterisk CLI verification sequence.
    5. Fill in the Result Log Table with actual dates/observations/pass-fail.
    Report back the completed table (or paste it directly) plus any failures encountered.
  </how-to-verify>
  <resume-signal>Paste the completed Result Log Table, or describe which rows failed and how.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 02-PHASE-SIGNOFF.md</name>
  <files>.planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md</files>
  <read_first>.planning/phases/01-push-wakeup-proof-of-concept/01-PHASE-SIGNOFF.md, tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md</read_first>
  <action>
    Using Task 2's reported results, write `.planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md` mirroring `01-PHASE-SIGNOFF.md`'s structure exactly (same section headers: "What Was Actually Proven", numbered gap sections, "Carried Forward"). Must include, at minimum:
    - **What Was Actually Proven:** Android CALL-01..05 results from Task 2's table (codecs, DTMF, hold/transfer, registration lifecycle); Android full unit test suite green (cite counts); cross-repo TLS/SRTP provisioning live (cite Plan 01 Task 3's checkpoint output); PJSIP 2.17 build verified on both platforms (Android real build in this sandbox per Plan 02, iOS structural + CI per Plan 03).
    - **D-09 mid-call network-switch:** report the actual outcome observed in Task 2 (pass/fail/partial), not assumed.
    - **D-15/D-16/D-17 iOS real-device gap (Open — Accepted):** explicitly state real audio I/O was never tested on a physical iPhone or a Simulator with real microphone/speaker hardware; only PJSIP build + `SipCallControllerTests.swift` unit-level verification (via CI, Plan 05) occurred.
    - **D-18 resumption trigger:** name the exact trigger verbatim -- "This gap closes when the user enrolls in the Apple Developer Program ($99/yr)" -- not a vague "later".
    - **D-10 correction note:** confirm the CLI verification used `pjsip show contacts`/`pjsip show aor`/`pjsip show endpoint`, not the non-existent `sip show registry`, and that this correction is now permanently reflected in the manual test procedure doc.
    - **Carried Forward:** list open items for later phases -- iOS real-device verification (blocks nothing in Phase 3, revisit before real iOS user install), any codec/routing issues found during Task 2 not yet fixed, DTLS-SRTP vs SDES open question (RESEARCH.md Open Question 2, revisit only if a reason emerges), the placeholder `<ha-phone-host>:5061` domain constants left in `HAPhoneTestApplication.kt`/`OutgoingCallActivity.kt` (Android) and `HAPhoneTestAppApp.swift` (iOS) that need replacing with the real box address once Plan 01's Task 3 checkpoint output is available (if not already substituted during execution).
  </action>
  <verify>
    <automated>grep -c "D-18" .planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md && grep -c "Apple Developer Program" .planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Apple Developer Program" .planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md` returns at least 1 (concrete resumption trigger present, D-18)
    - `grep -c "sip show registry" .planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md` returns 0 in any instructional context (only mentioned, if at all, as the corrected-away legacy command)
    - `grep -c "Carried Forward" .planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md` returns 1
    - `grep -c "## What Was Actually Proven" .planning/phases/02-pjsip-audio-media-core/02-PHASE-SIGNOFF.md` returns 1
  </acceptance_criteria>
  <done>02-PHASE-SIGNOFF.md documents exactly what was proven on Android, the real D-09 network-switch outcome, and the iOS real-device gap with a concrete, named resumption trigger -- not silently marked done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual test doc -> real credentials | The procedure doc records the real test extension's SIP password in plaintext for developer convenience |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-11 | Information Disclosure | `tools/docs/PHASE2_MANUAL_TEST_PROCEDURE.md`'s recorded test-extension password | accept | Scoped to a local-network-only, non-production dev/test extension (D-05/D-06) — same risk tier as Phase 1's dev-only signing key documentation; not a production credential |
</threat_model>

<verification>
Android + backend automated suites green; manual test matrix completed and reported by the user; 02-PHASE-SIGNOFF.md written with all D-09/D-10/D-15/D-17/D-18 items explicitly resolved or explicitly carried forward.
</verification>

<success_criteria>
Phase 2 has a documented, honest evidence trail: what was proven for real on Android, the exact corrected CLI verification for CALL-05, and a named, dated resumption trigger for the iOS real-device gap.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pjsip-audio-media-core/02-08-SUMMARY.md`
</output>
